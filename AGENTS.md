# Repository instructions

- Treat `wielicka.sh3d` as the only source of truth for the layout.
- Make every layout change in `wielicka.sh3d` first.
- Generate all derived renders, including files under `out/`, from `wielicka.sh3d`.
- Never hand-edit a render independently of the SH3D source.
